{
  "name": "Persona",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Persona name, e.g. Mobile User - AU"
    },
    "userAgent": {
      "type": "string",
      "description": "Browser user agent string"
    },
    "region": {
      "type": "string",
      "enum": [
        "au",
        "us-west-2",
        "us-east-1",
        "eu-central-1"
      ],
      "default": "au"
    },
    "useProxy": {
      "type": "boolean",
      "default": true
    },
    "proxyCountry": {
      "type": "string",
      "description": "ISO country code for geo proxy"
    },
    "deviceType": {
      "type": "string",
      "enum": [
        "desktop",
        "mobile",
        "tablet"
      ],
      "default": "desktop"
    },
    "notes": {
      "type": "string"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "name"
  ]
}