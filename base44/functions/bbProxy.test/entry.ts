// Placeholder — actual contract tests live in src/lib/bbProxy.contract.test.js
// (they run under Vitest, not Deno). This file exists only because the
// platform tried to deploy an earlier test file at this path.
Deno.serve(() => new Response('not a function', { status: 404 }));